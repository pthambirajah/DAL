@model IEnumerable<DTO.Dishes_order>

@{
    ViewData["Title"] = "Index";
}

<h2>Dishes By Order</h2>

<!--<p>
    <a asp-action="">Create New</a>
</p>-->
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.idDishes_Order)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.idDishes)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.idOrder)
            </th>

            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.idDishes_Order)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.idDishes)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.idOrder)
                </td>

                <!--
                  <td>
                    Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                    Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                    Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
                </td>-->
            </tr>
        }
    </tbody>
</table>


